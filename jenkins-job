#!/usr/bin/env groovy

pipeline {
    agent any
    options {
        skipDefaultCheckout true
    }
    stages {
        stage("first") {
            steps {
            //    sh "echo ${foo}"
                    echo "STAGE: first, STEP-1"
            }
        }
    }
}
