#!/usr/bin/env groovy

pipeline {
    agent any
    options {
        skipDefaultCheckout true
    }
    stages {
        stage("first") {
            steps {
            //    sh "echo ${foo}"
                step {
                    echo "STAGE: first, STEP-1"
                }
            }
        }
    }
}
