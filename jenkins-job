#!/usr/bin/env groovy

def myVar = "Hallo Otto"

pipeline {
    agent any
    options {
        skipDefaultCheckout true
    }
    stages {
        stage("first") {
            steps {
                    sh "echo ${myVar}"
                    echo "STAGE: first, STEP-1"
            }
        }
    }
}
